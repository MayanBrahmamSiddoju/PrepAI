name: CI — Success Check

on:
  push:
    branches: ["**"]

jobs:
  success-check:
    runs-on: ubuntu-latest
    steps:
      - name: Show message
        run: echo "Build successful — green tick shown!"
